// Barrel for all Mashinika domain tables. Import tables from here OR from
// "@/lib/db/schema" (which re-exports this barrel) ? the two are equivalent.
import { vehicles } from "@/lib/db/tables/vehicles"

export { vehicles }